# 스마트폰 설치 가이드

## 📱 스마트폰에 옮겨야 할 파일/폴더

### 🔥 필수 파일들 (전체 프로젝트)
```
simple_pdf_reader/           ← 전체 폴더를 옮기세요
├── android/                 ← Android 설정 및 빌드 파일
├── ios/                     ← iOS 설정 파일 (iOS용)
├── lib/                     ← Flutter 앱 소스코드
├── pubspec.yaml            ← 의존성 설정
├── build_and_install.bat   ← 자동 설치 스크립트
├── build_apk.bat          ← APK 빌드 스크립트
└── README.md              ← 사용법 안내
```

## 📋 설치 방법별 안내

### 방법 1: 전체 프로젝트로 개발 설치 (추천)
**옮길 것:** 전체 `simple_pdf_reader` 폴더
**장점:** 수정 가능, 디버깅 가능, 최신 기능 추가 가능

1. 전체 프로젝트 폴더를 컴퓨터로 복사
2. Flutter 개발환경 설치 (flutter.dev)
3. USB로 안드로이드 기기 연결
4. `build_and_install.bat` 실행

### 방법 2: APK 파일만 설치 (간단)
**옮길 것:** `app-release.apk` 파일 1개만
**장점:** 간단, 개발환경 불필요

1. 컴퓨터에서 `build_apk.bat` 실행
2. 생성된 `build/app/outputs/flutter-apk/app-release.apk` 파일만 스마트폰으로 복사
3. 스마트폰에서 APK 파일 실행하여 설치

## 🔧 개발환경 설치 (방법 1 선택시)

### Windows에서 Flutter 설치
```bash
# 1. Flutter SDK 다운로드
# https://flutter.dev/docs/get-started/install/windows

# 2. 환경변수 PATH에 Flutter 추가
# C:\flutter\bin

# 3. 의존성 확인
flutter doctor

# 4. Android Studio 설치 (선택사항)
```

### 안드로이드 기기 준비
1. **설정 > 휴대전화 정보 > 빌드번호** 7번 터치 (개발자 옵션 활성화)
2. **설정 > 개발자 옵션 > USB 디버깅** 활성화
3. USB로 컴퓨터 연결
4. 디버깅 허용 팝업에서 "허용" 선택

## 📱 APK 직접 설치 (방법 2)

### 안드로이드 기기에서
1. **설정 > 보안 > 알 수 없는 소스** 허용
2. 파일 관리자에서 APK 파일 찾기
3. APK 파일 터치하여 설치
4. 설치 완료 후 앱 실행

## ⚠️ 주의사항

- **전체 프로젝트 방법**: Flutter 개발환경 필요, 하지만 수정/업데이트 가능
- **APK만 설치**: 개발환경 불필요, 하지만 수정 불가능
- iOS는 Apple Developer 계정 필요 (유료)
- 안드로이드 권한 허용 필요 (파일 접근)

## 🚀 빠른 시작

**가장 쉬운 방법 (APK):**
1. 컴퓨터에서 `build_apk.bat` 실행
2. `app-release.apk` 파일을 스마트폰으로 복사
3. 스마트폰에서 설치

**개발자 방법 (전체 프로젝트):**
1. Flutter 설치
2. 전체 폴더 복사
3. `build_and_install.bat` 실행